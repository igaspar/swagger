'use strict';

var url = require('url');

var Layout = require('./LayoutService');

module.exports.file Definition POST = function file Definition POST (req, res, next) {
  Layout.file Definition POST(req.swagger.params, res, next);
};
